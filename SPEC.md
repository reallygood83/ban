# 반 배정 프로그램 Pro - 웹앱 개발 사양서

## 📋 프로젝트 개요

### 프로젝트 정보
- **프로젝트명**: 반 배정 프로그램 Pro (Classroom Assignment Tool Pro)
- **버전**: v2.0 (Web Application)
- **목적**: 학교 학급 편성을 위한 자동화된 배정 시스템
- **배포 플랫폼**: Vercel
- **저장소**: https://github.com/reallygood83/ban
- **디자인 컨셉**: Neo-Brutalism (네오 부루탈리즘)

### 핵심 목표
1. 교사들이 손쉽게 학급 배정을 수행할 수 있는 직관적 UI 제공
2. Google 로그인을 통한 안전한 인증 시스템
3. Firebase를 활용한 실시간 데이터 동기화
4. Gemini AI를 활용한 지능형 배정 최적화
5. 매력적인 네오 부루탈리즘 디자인으로 사용자 경험 극대화

---

## 🎨 디자인 시스템 - Neo-Brutalism

### 디자인 철학
- **대담한 색상**: 고채도의 원색 사용 (검정, 노랑, 빨강, 파랑, 초록)
- **두꺼운 테두리**: 모든 요소에 3-5px의 검은색 테두리
- **강렬한 그림자**: 하드 그림자 (4-8px offset, no blur)
- **기하학적 형태**: 정사각형, 원형 등 명확한 도형
- **타이포그래피**: 굵고 읽기 쉬운 산세리프 폰트
- **인터랙션**: 과감한 호버 효과 (색상 변화, 위치 이동)

### 컬러 팔레트
```css
--primary-yellow: #FFDE59
--primary-red: #FF6B6B
--primary-blue: #4ECDC4
--primary-green: #95E1D3
--primary-purple: #C7CEEA
--neutral-black: #2D3142
--neutral-white: #FFFFFF
--background: #F8F9FA
--border: #2D3142
```

### 타이포그래피
- **Primary Font**: "Space Grotesk", "Pretendard", sans-serif
- **Heading**: 700-900 weight
- **Body**: 500-600 weight
- **Button**: 700 weight

### UI 컴포넌트 스타일 가이드
- **버튼**: 두꺼운 테두리 + 하드 그림자 + 호버시 이동 효과
- **카드**: 배경색 + 검은 테두리 + 오프셋 그림자
- **입력 필드**: 흰 배경 + 검은 테두리 + 포커스시 색상 테두리
- **모달**: 중앙 정렬 + 두꺼운 테두리 + 배경 블러
- **네비게이션**: 고정 상단 바 + 대담한 아이콘 + 활성 상태 색상 표시

---

## 🏗️ 기술 스택

### Frontend
- **Framework**: React 19.2.0 + TypeScript 5.8.2
- **Build Tool**: Vite 6.2.0
- **Styling**: Tailwind CSS 3.4+ (Neo-Brutalism 커스텀 설정)
- **UI Icons**: Lucide React 0.554.0
- **State Management**: React Context API + Local Storage
- **Routing**: React Router v6

### Backend & Services
- **Authentication**: Firebase Authentication (Google OAuth)
- **Database**: Firebase Firestore
- **Storage**: Firebase Storage (프로필 이미지 등)
- **Analytics**: Firebase Analytics
- **Hosting**: Vercel

### AI Integration
- **AI Model**: Google Gemini 2.0 Flash
- **API**: BYOK (Bring Your Own Key) - 사용자 설정
- **용도**: 지능형 학급 배정 추천, 최적화 알고리즘

### Development Tools
- **Version Control**: Git + GitHub
- **CI/CD**: Vercel Auto Deploy
- **Code Quality**: ESLint + Prettier
- **Type Checking**: TypeScript Strict Mode

---

## 📐 시스템 아키텍처

### 애플리케이션 구조
```
ban-main/
├── public/
│   ├── favicon.ico
│   └── og-image.png
├── src/
│   ├── assets/
│   │   └── images/
│   ├── components/
│   │   ├── auth/
│   │   │   ├── GoogleLoginButton.tsx
│   │   │   └── AuthGuard.tsx
│   │   ├── layout/
│   │   │   ├── Header.tsx
│   │   │   ├── Footer.tsx
│   │   │   └── Navigation.tsx
│   │   ├── landing/
│   │   │   ├── Hero.tsx
│   │   │   ├── Features.tsx
│   │   │   ├── HowItWorks.tsx
│   │   │   └── CTA.tsx
│   │   ├── assignment/
│   │   │   ├── InputPhase.tsx
│   │   │   ├── StudentDetails.tsx
│   │   │   ├── RequestManager.tsx
│   │   │   ├── SettingsPhase.tsx
│   │   │   └── ResultsPhase.tsx
│   │   ├── settings/
│   │   │   ├── ProfileSettings.tsx
│   │   │   ├── GeminiAPISettings.tsx
│   │   │   └── DataManagement.tsx
│   │   └── ui/
│   │       ├── Button.tsx
│   │       ├── Card.tsx
│   │       ├── Input.tsx
│   │       ├── Modal.tsx
│   │       └── StepWizard.tsx
│   ├── contexts/
│   │   ├── AuthContext.tsx
│   │   ├── AssignmentContext.tsx
│   │   └── SettingsContext.tsx
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useFirestore.ts
│   │   ├── useGemini.ts
│   │   └── useLocalStorage.ts
│   ├── lib/
│   │   ├── firebase.ts
│   │   ├── gemini.ts
│   │   └── utils.ts
│   ├── pages/
│   │   ├── Landing.tsx
│   │   ├── Dashboard.tsx
│   │   ├── Assignment.tsx
│   │   ├── History.tsx
│   │   └── Settings.tsx
│   ├── services/
│   │   ├── auth.service.ts
│   │   ├── assignment.service.ts
│   │   ├── firestore.service.ts
│   │   └── gemini.service.ts
│   ├── types/
│   │   ├── index.ts
│   │   ├── auth.types.ts
│   │   └── assignment.types.ts
│   ├── utils/
│   │   ├── algorithm.ts
│   │   ├── validation.ts
│   │   └── export.ts
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css
├── .env.example
├── .gitignore
├── package.json
├── tailwind.config.js
├── tsconfig.json
├── vite.config.ts
└── vercel.json
```

### Firebase 데이터 구조

#### Firestore Collections

**users** (사용자 정보)
```typescript
{
  uid: string;
  email: string;
  displayName: string;
  photoURL: string;
  schoolName?: string;
  geminiApiKey?: string; // 암호화 저장
  createdAt: timestamp;
  lastLogin: timestamp;
}
```

**assignments** (배정 프로젝트)
```typescript
{
  id: string;
  userId: string;
  projectName: string;
  grade: number;
  classCount: number;
  students: Student[];
  requests: Request[];
  settings: AssignmentSettings;
  results?: AssignmentResult[];
  status: 'draft' | 'completed';
  createdAt: timestamp;
  updatedAt: timestamp;
}
```

**history** (배정 기록)
```typescript
{
  id: string;
  userId: string;
  assignmentId: string;
  action: 'create' | 'update' | 'run' | 'export';
  timestamp: timestamp;
  metadata: object;
}
```

---

## 🌐 페이지 구성

### 1. Landing Page (랜딩 페이지)
**경로**: `/`

**섹션**:
1. **Hero Section**
   - 대담한 헤드라인: "학급 편성, 이제는 AI와 함께!"
   - 3D 효과의 CTA 버튼 (Google 로그인)
   - 네오 부루탈리즘 스타일의 일러스트레이션
   - 배경: 기하학적 패턴 + 강렬한 색상 블록

2. **Features Section**
   - 3개 주요 기능 카드 (AI 추천, 실시간 협업, 자동 균형)
   - 각 카드: 아이콘 + 제목 + 설명
   - 호버시 카드 이동 + 색상 변경 효과

3. **How It Works Section**
   - 5단계 프로세스 타임라인
   - 각 단계마다 네오 부루탈리즘 일러스트
   - 번호 박스: 큰 숫자 + 하드 그림자

4. **Testimonials Section**
   - 교사 후기 카드 (3개)
   - 프로필 이미지 + 이름 + 학교 + 코멘트
   - 대담한 따옴표 디자인

5. **CTA Section**
   - "지금 시작하기" 대형 버튼
   - 무료 체험 강조
   - 배경: 대각선 색상 블록

**디자인 특징**:
- 스크롤 애니메이션 (Intersection Observer)
- 패럴랙스 효과 (배경 레이어)
- 마이크로 인터랙션 (버튼, 카드 호버)

### 2. Dashboard (대시보드)
**경로**: `/dashboard`
**인증**: 필수 (Google 로그인)

**구성**:
- **상단 바**: 사용자 프로필 + 학교명 + 설정 버튼
- **통계 카드**: 총 프로젝트 수, 완료된 배정, 이번 달 활동
- **최근 프로젝트**: 카드 그리드 (6개 표시)
  - 프로젝트명, 학년, 반 수, 학생 수, 상태, 날짜
  - 액션 버튼: 편집, 삭제, 복제
- **빠른 시작 버튼**: "+ 새 배정 시작하기" (대형 CTA)

**디자인**:
- 그리드 레이아웃 (카드 간격 24px)
- 각 카드: 흰 배경 + 검은 테두리 + 오프셋 그림자
- 상태 태그: 색상 코딩 (Draft: 노랑, Completed: 초록)

### 3. Assignment Page (배정 페이지)
**경로**: `/assignment/:id?`
**인증**: 필수

**5단계 위저드**:

#### Step 1: 기본 설정 & 명단
- 프로젝트명 입력 (대형 인풋)
- 학년/반 수 선택 (드롭다운)
- 학생 명단 입력 방법:
  - 직접 입력 (테이블 형태)
  - Excel 업로드 (드래그 앤 드롭)
  - Firebase에서 불러오기
- 미리보기 테이블 (정렬, 필터링)

#### Step 2: 비고 입력
- 학생 목록 (카드 형태)
- 각 학생 카드:
  - 이름, 성별, 현재 반/번호
  - 비고 유형 선택 (드롭다운)
  - 상세 내용 입력 (텍스트 영역)
- 필터 버튼: 전체 / 비고 있음 / 특수학급

#### Step 3: 분리/통합 요청
- 요청 추가 버튼 (+ 모달 오픈)
- 요청 목록 테이블:
  - 유형, 학생1, 학생2, 사유
  - 편집/삭제 액션
- 드래그 앤 드롭으로 학생 선택 지원

#### Step 4: 배정 설정
- **배정 방법**: 라디오 버튼 (순환, 지그재그, 랜덤)
- **동명이인 처리**: 토글 스위치
- **특수학급 감원**: 슬라이더 (-1 ~ -3명)
- **비고 우선순위**: 드래그 앤 드롭으로 순서 조정
- **시작 설정**:
  - 현재 반별 시작 학생 선택
  - 타겟 반 선택
- **AI 추천 버튼**: Gemini API 활용 최적화 제안

#### Step 5: 결과 확인
- **3단계 결과 탭**: 초기 배정 / 제약 반영 / 균형 조정
- **통계 대시보드**:
  - 반별 학생 수, 성비, 특수학급 분포
  - 차트 (Bar Chart, Pie Chart)
- **상세 명단 테이블**:
  - 하이라이트 (노랑: 제약 이동, 파랑: 균형 이동)
  - 이동 로그 툴팁
- **액션 버튼**:
  - 재실행, Excel 내보내기, PDF 내보내기, 저장

**네비게이션**:
- 상단 단계 인디케이터 (클릭 가능)
- 하단 버튼: 이전, 다음, 저장

### 4. History Page (기록 페이지)
**경로**: `/history`
**인증**: 필수

**구성**:
- 타임라인 형태로 활동 기록 표시
- 각 기록 카드: 날짜, 액션, 프로젝트명, 상세 내용
- 필터: 전체 / 생성 / 수정 / 실행 / 내보내기
- 검색 바

### 5. Settings Page (설정 페이지)
**경로**: `/settings`
**인증**: 필수

**탭 구성**:

#### 1. 프로필 설정
- 프로필 이미지 업로드
- 이름, 이메일 (읽기 전용)
- 학교명 입력
- 로그아웃 버튼

#### 2. Gemini API 설정
- **API 키 입력 섹션**:
  - 인풋 필드 (비밀번호 타입)
  - "저장" 버튼
  - 보안 안내 메시지
- **연결 상태 표시**:
  - 초록 불: 연결됨
  - 빨강 불: 연결 안 됨
- **테스트 버튼**: API 키 유효성 검증
- **사용량 표시**: 이번 달 API 호출 수 (추정)

#### 3. 데이터 관리
- **백업**: Firebase에서 전체 데이터 내보내기 (JSON)
- **복원**: JSON 파일 업로드로 데이터 복원
- **삭제**: 모든 프로젝트 삭제 (확인 모달)

---

## 🔐 인증 시스템

### Firebase Authentication 구현

#### Google OAuth 로그인 플로우
1. 사용자가 "Google로 로그인" 버튼 클릭
2. Firebase Auth Popup 오픈
3. Google 계정 선택 및 권한 승인
4. Firebase에서 사용자 정보 반환
5. Firestore에 사용자 문서 생성/업데이트
6. AuthContext에 사용자 정보 저장
7. Dashboard로 리다이렉트

#### 보호된 라우트 (AuthGuard)
```tsx
// AuthGuard Component
- 인증 상태 확인
- 미인증 시 Landing으로 리다이렉트
- 로딩 중 스피너 표시
```

#### 세션 관리
- Firebase Auth의 자동 세션 유지
- 로컬 스토리지에 마지막 로그인 시간 저장
- 30일 무활동 시 재로그인 요청

---

## 🤖 AI 통합 - Gemini 2.0 Flash

### BYOK (Bring Your Own Key) 시스템

#### API 키 저장 방식
1. **사용자 입력**: Settings 페이지에서 API 키 입력
2. **암호화**: AES-256 암호화 (클라이언트 측)
3. **저장**: Firestore `users` 컬렉션에 암호화된 키 저장
4. **사용 시 복호화**: 요청 시에만 메모리에서 복호화

#### API 키 유효성 검증
```typescript
// Gemini API Test Request
- 간단한 프롬프트 전송
- 응답 성공 시 "연결됨" 표시
- 실패 시 에러 메시지 표시
```

### AI 기능 활용

#### 1. 지능형 배정 추천
**프롬프트 예시**:
```
학생 데이터를 분석하여 최적의 학급 배정을 추천해주세요.

학생 수: 60명
반 수: 3개
특수 요구사항:
- 특수학급 학생: 3명
- 분리 요청: 5건
- 통합 요청: 2건

각 반의 균형을 고려하여 추천해주세요.
```

**응답 활용**:
- AI 추천 결과를 Settings Phase에 자동 적용
- 사용자가 수정 가능

#### 2. 배정 결과 최적화
**프롬프트 예시**:
```
현재 배정 결과를 분석하고 개선점을 제안해주세요.

현재 배정:
- 가반: 20명 (남 12, 여 8, 특수 2)
- 나반: 19명 (남 10, 여 9, 특수 0)
- 다반: 21명 (남 11, 여 10, 특수 1)

균형을 위한 조정 제안을 해주세요.
```

**응답 활용**:
- 조정 제안 표시
- 원클릭 적용 버튼

#### 3. 제약 조건 충돌 해결
**프롬프트 예시**:
```
다음 분리/통합 요청 간 충돌이 발생했습니다:
- 학생 A와 B: 분리 요청 (학교폭력)
- 학생 B와 C: 통합 요청 (쌍둥이)

최적의 해결 방안을 제안해주세요.
```

**응답 활용**:
- 충돌 해결 방안 표시
- 우선순위 자동 조정

---

## 🎨 네오 부루탈리즘 UI 컴포넌트 설계

### 공통 컴포넌트 스타일

#### Button Component
```tsx
// Primary Button
- Background: --primary-yellow
- Border: 4px solid --neutral-black
- Shadow: 6px 6px 0px --neutral-black
- Hover: translate(-2px, -2px), shadow 8px 8px 0px
- Font: 700 weight, uppercase
- Padding: 16px 32px
- Border-radius: 8px

// Secondary Button
- Background: --neutral-white
- Border: 4px solid --neutral-black
- Same shadow & hover effects

// Danger Button
- Background: --primary-red
- Color: --neutral-white
```

#### Card Component
```tsx
- Background: gradient or solid color
- Border: 4px solid --neutral-black
- Shadow: 8px 8px 0px --neutral-black
- Padding: 24px
- Border-radius: 12px
- Hover: slight rotation (1-2deg)
```

#### Input Component
```tsx
- Background: --neutral-white
- Border: 3px solid --neutral-black
- Focus: border-color changes to theme color
- Padding: 12px 16px
- Font: 500 weight
- Border-radius: 8px
```

#### Modal Component
```tsx
- Overlay: rgba(0,0,0,0.5) backdrop blur
- Content: white background
- Border: 5px solid --neutral-black
- Shadow: 12px 12px 0px --neutral-black
- Max-width: 600px
- Padding: 32px
- Close button: X icon with hover effect
```

#### Table Component
```tsx
- Border: 3px solid --neutral-black
- Header: --primary-blue background
- Row hover: --background color
- Cell padding: 12px
- Alternating row colors: subtle
```

### Landing Page 특수 컴포넌트

#### Hero Animation
```tsx
// 3D Floating Cards
- Multiple layers with parallax
- Rotation on scroll
- Color-changing borders
- Animated shadows
```

#### Feature Cards
```tsx
// Card Tilt on Hover
- 3D perspective transform
- Shadow movement
- Icon bounce animation
```

#### Step Indicators
```tsx
// Large Number Circles
- Circle: 80px diameter
- Border: 4px solid
- Number: 700 weight, 32px
- Background: theme color
- Shadow: 6px 6px 0px
```

---

## 📦 배포 및 환경 설정

### Vercel 배포 설정

#### vercel.json
```json
{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/index.html"
    }
  ]
}
```

#### 환경 변수 (Vercel Dashboard)
```
VITE_FIREBASE_API_KEY=AIzaSyDJ35oGlefgLuQLUvfKpHYHiF0f7wzcVxg
VITE_FIREBASE_AUTH_DOMAIN=goodbye-9ea05.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=goodbye-9ea05
VITE_FIREBASE_STORAGE_BUCKET=goodbye-9ea05.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=362700106616
VITE_FIREBASE_APP_ID=1:362700106616:web:3af06530023d5c4bddf68d
VITE_FIREBASE_MEASUREMENT_ID=G-9KJYWNX6WC
```

### Git 보안 설정

#### .gitignore
```
# Environment Variables
.env
.env.local
.env.production

# Firebase Config (if not using env vars)
src/lib/firebase.config.ts

# Build
dist/
build/

# Dependencies
node_modules/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Testing
coverage/
```

#### .env.example
```
# Firebase Configuration
VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain_here
VITE_FIREBASE_PROJECT_ID=your_project_id_here
VITE_FIREBASE_STORAGE_BUCKET=your_storage_bucket_here
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id_here
VITE_FIREBASE_APP_ID=your_app_id_here
VITE_FIREBASE_MEASUREMENT_ID=your_measurement_id_here
```

### Firebase 보안 규칙

#### Firestore Rules
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users collection
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Assignments collection
    match /assignments/{assignmentId} {
      allow read, write: if request.auth != null &&
        resource.data.userId == request.auth.uid;
      allow create: if request.auth != null;
    }

    // History collection
    match /history/{historyId} {
      allow read: if request.auth != null &&
        resource.data.userId == request.auth.uid;
      allow create: if request.auth != null;
    }
  }
}
```

#### Storage Rules
```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /profile-images/{userId}/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId
        && request.resource.size < 5 * 1024 * 1024 // 5MB limit
        && request.resource.contentType.matches('image/.*');
    }
  }
}
```

---

## 🧪 개발 단계별 계획

### Phase 1: 프로젝트 초기 설정 (1주차)
**목표**: 개발 환경 구축 및 기본 구조 생성

**작업 항목**:
- [ ] Git 저장소 초기화 및 GitHub 연동
- [ ] Vite + React + TypeScript 프로젝트 생성
- [ ] Tailwind CSS 설치 및 네오 부루탈리즘 테마 설정
- [ ] 폴더 구조 생성
- [ ] Firebase 프로젝트 연동
- [ ] Vercel 프로젝트 생성 및 연동
- [ ] 환경 변수 설정 (.env.example 작성)
- [ ] 기본 라우팅 설정 (React Router)
- [ ] 공통 UI 컴포넌트 개발 (Button, Card, Input)

**완료 기준**:
- 로컬에서 개발 서버 실행 가능
- Vercel에 기본 페이지 배포 성공
- Firebase 연결 확인

### Phase 2: 인증 시스템 구현 (1주차)
**목표**: Google 로그인 및 사용자 관리

**작업 항목**:
- [ ] Firebase Authentication 설정
- [ ] Google OAuth Provider 활성화
- [ ] AuthContext 구현
- [ ] GoogleLoginButton 컴포넌트 개발
- [ ] AuthGuard HOC 구현
- [ ] 로그인/로그아웃 플로우 구현
- [ ] Firestore 사용자 문서 자동 생성
- [ ] 프로필 설정 페이지 개발

**완료 기준**:
- Google 로그인 성공
- 사용자 정보 Firestore 저장 확인
- 보호된 라우트 정상 작동

### Phase 3: Landing Page 개발 (1주차)
**목표**: 매력적인 네오 부루탈리즘 랜딩 페이지

**작업 항목**:
- [ ] Hero Section 컴포넌트 개발
  - 헤드라인, 서브 헤드라인
  - CTA 버튼 (Google 로그인)
  - 3D 일러스트레이션 or 애니메이션
- [ ] Features Section 컴포넌트 개발
  - 3개 주요 기능 카드
  - 아이콘 + 설명
  - 호버 애니메이션
- [ ] How It Works Section 컴포넌트 개발
  - 5단계 타임라인
  - 일러스트레이션
- [ ] Testimonials Section 컴포넌트 개발
  - 후기 카드 (3개)
- [ ] CTA Section 컴포넌트 개발
- [ ] 스크롤 애니메이션 구현 (Intersection Observer)
- [ ] 반응형 디자인 최적화

**완료 기준**:
- 모든 섹션 완성
- 애니메이션 정상 작동
- 모바일/태블릿/데스크톱 반응형 확인

### Phase 4: Dashboard 개발 (1주차)
**목표**: 사용자 대시보드 및 프로젝트 관리

**작업 항목**:
- [ ] Dashboard 레이아웃 구성
- [ ] Header 컴포넌트 (프로필, 설정 버튼)
- [ ] 통계 카드 컴포넌트
- [ ] 프로젝트 카드 그리드
- [ ] 새 프로젝트 생성 모달
- [ ] 프로젝트 CRUD 기능
  - 생성, 조회, 수정, 삭제
- [ ] Firestore 쿼리 구현
- [ ] 로딩 상태 및 에러 처리

**완료 기준**:
- 프로젝트 생성/조회/수정/삭제 정상 작동
- Firestore 데이터 실시간 동기화 확인

### Phase 5: Assignment Wizard - Step 1 & 2 (1주차)
**목표**: 학생 명단 입력 및 비고 입력

**작업 항목**:
- [ ] StepWizard 컴포넌트 개선
- [ ] InputPhase 컴포넌트 개발
  - 프로젝트명 입력
  - 학년/반 수 선택
  - 학생 명단 직접 입력 (테이블)
  - Excel 파일 업로드 파싱
  - 유효성 검증
- [ ] StudentDetails 컴포넌트 개발
  - 학생 카드 형태
  - 비고 유형 선택
  - 상세 내용 입력
  - 필터링 기능
- [ ] 데이터 상태 관리 (AssignmentContext)
- [ ] 로컬 스토리지 자동 저장 (초안)

**완료 기준**:
- 학생 명단 수동/파일 입력 정상 작동
- 비고 입력 및 저장 확인

### Phase 6: Assignment Wizard - Step 3 & 4 (1주차)
**목표**: 분리/통합 요청 및 배정 설정

**작업 항목**:
- [ ] RequestManager 컴포넌트 개발
  - 요청 추가 모달
  - 학생 선택 (드롭다운 or 검색)
  - 요청 목록 테이블
  - 편집/삭제 기능
- [ ] SettingsPhase 컴포넌트 개발
  - 배정 방법 선택 (라디오 버튼)
  - 동명이인 처리 (토글)
  - 특수학급 감원 (슬라이더)
  - 비고 우선순위 (드래그 앤 드롭)
  - 시작 설정 UI
- [ ] 유효성 검증 로직
- [ ] Gemini API 연동 준비

**완료 기준**:
- 분리/통합 요청 CRUD 정상 작동
- 배정 설정 저장 확인

### Phase 7: Gemini AI 통합 (1주차)
**목표**: AI 기반 추천 시스템

**작업 항목**:
- [ ] Gemini API 서비스 모듈 개발
- [ ] API 키 암호화/복호화 로직
- [ ] Settings 페이지 - Gemini API 설정 UI
  - API 키 입력
  - 연결 테스트
  - 상태 표시
- [ ] AI 추천 기능 구현
  - 프롬프트 엔지니어링
  - 응답 파싱
  - 결과 적용
- [ ] 에러 처리 (API 한도, 네트워크 오류)

**완료 기준**:
- API 키 저장 및 사용 정상 작동
- AI 추천 결과 정상 출력

### Phase 8: Assignment Algorithm (2주차)
**목표**: 핵심 배정 알고리즘 구현

**작업 항목**:
- [ ] 기존 utils.ts의 알고리즘 리팩토링
- [ ] 3단계 배정 로직 구현
  - Step 1: 초기 배정 (순환/지그재그/랜덤)
  - Step 2: 제약 조건 반영 (분리/통합)
  - Step 3: 균형 조정 (성비, 특수학급)
- [ ] 충돌 감지 및 해결 로직
- [ ] 로그 생성 기능
- [ ] 성능 최적화 (대량 학생 처리)

**완료 기준**:
- 100명 이상 학생 배정 정상 작동
- 제약 조건 100% 충족 확인

### Phase 9: Results Phase (1주차)
**목표**: 배정 결과 시각화 및 관리

**작업 항목**:
- [ ] ResultsPhase 컴포넌트 개발
- [ ] 3단계 탭 UI
- [ ] 통계 대시보드
  - 반별 학생 수 차트 (Bar)
  - 성비 차트 (Pie)
  - 특수학급 분포
- [ ] 상세 명단 테이블
  - 하이라이트 (색상 코딩)
  - 이동 로그 툴팁
  - 정렬/필터링
- [ ] Excel 내보내기 기능 (SheetJS)
- [ ] PDF 내보내기 기능 (jsPDF)
- [ ] 재실행 기능

**완료 기준**:
- 결과 시각화 정상 표시
- 파일 내보내기 정상 작동

### Phase 10: History & Settings (1주차)
**목표**: 기록 관리 및 사용자 설정

**작업 항목**:
- [ ] History 페이지 개발
  - 타임라인 UI
  - 활동 기록 카드
  - 필터/검색 기능
- [ ] Settings 페이지 완성
  - 프로필 설정 (이미지 업로드)
  - 학교명 입력
  - 데이터 관리 (백업/복원/삭제)
- [ ] Firebase Storage 프로필 이미지 업로드
- [ ] JSON 내보내기/가져오기 기능

**완료 기준**:
- 활동 기록 정상 표시
- 프로필 이미지 업로드 성공
- 데이터 백업/복원 정상 작동

### Phase 11: 최적화 및 테스트 (1주차)
**목표**: 성능 최적화 및 버그 수정

**작업 항목**:
- [ ] 코드 스플리팅 (React.lazy)
- [ ] 이미지 최적화 (WebP 변환)
- [ ] Lighthouse 성능 점수 개선
- [ ] 접근성 (a11y) 검증
- [ ] 크로스 브라우저 테스트
- [ ] 모바일 디바이스 테스트
- [ ] 에러 바운더리 구현
- [ ] 로딩 스켈레톤 UI 추가
- [ ] 버그 수정 및 리팩토링

**완료 기준**:
- Lighthouse 점수 90+ (Performance, Accessibility)
- 주요 브라우저 호환성 확인

### Phase 12: 배포 및 문서화 (1주차)
**목표**: 프로덕션 배포 및 사용자 가이드

**작업 항목**:
- [ ] Vercel 프로덕션 배포
- [ ] 커스텀 도메인 설정 (선택)
- [ ] README.md 작성
  - 프로젝트 소개
  - 기능 설명
  - 설치 가이드
  - 환경 변수 설정
  - 스크린샷
- [ ] 사용자 가이드 문서 작성
- [ ] API 문서 작성 (JSDoc)
- [ ] 라이선스 파일 추가
- [ ] 릴리즈 노트 작성

**완료 기준**:
- 프로덕션 배포 성공
- 문서 완성

---

## 📊 성능 목표

### Lighthouse 점수 목표
- **Performance**: 90+
- **Accessibility**: 95+
- **Best Practices**: 95+
- **SEO**: 90+

### 핵심 성능 지표
- **First Contentful Paint (FCP)**: < 1.5s
- **Largest Contentful Paint (LCP)**: < 2.5s
- **Time to Interactive (TTI)**: < 3.5s
- **Cumulative Layout Shift (CLS)**: < 0.1
- **First Input Delay (FID)**: < 100ms

### 최적화 전략
- 코드 스플리팅 (React.lazy, Suspense)
- 이미지 최적화 (WebP, 적절한 크기)
- CDN 활용 (Vercel Edge Network)
- Service Worker (PWA, 선택)
- Gzip/Brotli 압축
- Critical CSS 인라인

---

## 🔒 보안 고려사항

### 데이터 보호
1. **API 키 암호화**: Gemini API 키는 클라이언트에서 AES-256 암호화 후 Firestore 저장
2. **환경 변수**: Firebase 설정은 Vercel 환경 변수로만 관리
3. **HTTPS**: Vercel 자동 HTTPS 적용
4. **CSP 헤더**: Content Security Policy 설정

### Firebase 보안
1. **Firestore Rules**: 사용자 인증 및 소유권 검증
2. **Storage Rules**: 파일 크기 및 타입 제한
3. **Auth 제한**: 이메일 인증 필수 (선택)

### 코드 보안
1. **XSS 방지**: React의 자동 이스케이프 + DOMPurify (필요시)
2. **CSRF 방지**: Firebase Auth 토큰 기반
3. **Input Validation**: 클라이언트 & 서버 측 모두 검증

---

## 📱 반응형 디자인

### 브레이크포인트
```css
sm: 640px   /* 모바일 가로 */
md: 768px   /* 태블릿 */
lg: 1024px  /* 노트북 */
xl: 1280px  /* 데스크톱 */
2xl: 1536px /* 대형 모니터 */
```

### 디바이스별 UI 조정
- **모바일 (< 640px)**:
  - 햄버거 메뉴
  - 1열 그리드
  - 터치 친화적 버튼 크기 (최소 44px)
  - 스와이프 제스처 지원

- **태블릿 (640-1024px)**:
  - 2열 그리드
  - 사이드바 토글

- **데스크톱 (> 1024px)**:
  - 3-4열 그리드
  - 영구 사이드바
  - 호버 효과 강화

---

## 🧰 개발 도구 및 라이브러리

### 필수 패키지
```json
{
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-router-dom": "^6.28.0",
    "firebase": "^11.0.0",
    "@google/generative-ai": "^0.21.0",
    "lucide-react": "^0.554.0",
    "clsx": "^2.1.1",
    "framer-motion": "^11.15.0",
    "react-dropzone": "^14.3.5",
    "xlsx": "^0.18.5",
    "jspdf": "^2.5.2",
    "jspdf-autotable": "^3.8.4",
    "recharts": "^2.15.0",
    "react-beautiful-dnd": "^13.1.1",
    "crypto-js": "^4.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@types/crypto-js": "^4.2.2",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0",
    "tailwindcss": "^3.4.17",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "eslint": "^9.18.0",
    "prettier": "^3.4.2"
  }
}
```

### VS Code 추천 확장
- ESLint
- Prettier
- Tailwind CSS IntelliSense
- Firebase Explorer
- TypeScript Hero

---

## 📝 Git 워크플로우

### 브랜치 전략
```
main (프로덕션)
└── develop (개발)
    ├── feature/auth (인증 기능)
    ├── feature/landing (랜딩 페이지)
    ├── feature/assignment (배정 기능)
    └── feature/ai (AI 통합)
```

### 커밋 컨벤션
```
feat: 새로운 기능 추가
fix: 버그 수정
docs: 문서 수정
style: 코드 스타일 변경 (포맷팅)
refactor: 코드 리팩토링
test: 테스트 추가/수정
chore: 빌드 프로세스 수정
```

**예시**:
```
feat: Add Google OAuth login
fix: Fix student table sorting issue
docs: Update README with setup instructions
```

---

## 🎯 성공 지표 (KPI)

### 사용자 지표
- **월간 활성 사용자 (MAU)**: 목표 100명 (첫 3개월)
- **사용자 유지율**: 60% 이상
- **평균 세션 시간**: 15분 이상
- **배정 완료율**: 80% 이상

### 기술 지표
- **페이지 로드 시간**: 3초 이내
- **API 응답 시간**: 2초 이내
- **에러율**: 1% 미만
- **Uptime**: 99.9% 이상

### 비즈니스 지표
- **NPS (Net Promoter Score)**: 50+ (우수)
- **사용자 피드백 수집**: 월 10건 이상
- **기능 요청 처리율**: 50% 이상

---

## 🚀 출시 후 로드맵

### v2.1 (1개월 후)
- [ ] 협업 기능 (여러 교사가 하나의 프로젝트 편집)
- [ ] 알림 시스템 (Firebase Cloud Messaging)
- [ ] 프로젝트 템플릿 (자주 사용하는 설정 저장)

### v2.2 (2개월 후)
- [ ] 다국어 지원 (영어, 일본어)
- [ ] 테마 변경 (다크 모드, 색상 커스터마이징)
- [ ] 통계 리포트 (연도별, 학년별 분석)

### v2.3 (3개월 후)
- [ ] 모바일 앱 (React Native)
- [ ] 오프라인 모드 (Service Worker)
- [ ] 고급 AI 기능 (학생 성향 분석, 최적 조합 예측)

### v3.0 (6개월 후)
- [ ] 관리자 대시보드 (학교 전체 관리)
- [ ] 학부모 포털 (자녀 배정 결과 조회)
- [ ] API 공개 (다른 학교 시스템과 연동)

---

## 📞 지원 및 문의

### 문서
- **GitHub Wiki**: 상세 사용 가이드
- **FAQ**: 자주 묻는 질문
- **Changelog**: 업데이트 기록

### 커뮤니티
- **GitHub Discussions**: 기능 제안, 질문
- **Issue Tracker**: 버그 리포트

### 연락처
- **개발자**: GitHub @reallygood83
- **이메일**: (추가 예정)

---

## 📄 라이선스

MIT License

---

## ✅ 체크리스트 요약

### 개발 시작 전
- [ ] GitHub 저장소 생성 및 권한 설정
- [ ] Vercel 프로젝트 생성
- [ ] Firebase 프로젝트 설정
- [ ] 환경 변수 Vercel에 등록
- [ ] 로컬 개발 환경 구축

### 개발 중
- [ ] .gitignore에 민감 정보 추가 확인
- [ ] 환경 변수 하드코딩 금지
- [ ] 커밋 전 코드 리뷰
- [ ] Vercel 프리뷰 배포 확인

### 배포 전
- [ ] 프로덕션 빌드 테스트
- [ ] Firebase 보안 규칙 검증
- [ ] Lighthouse 점수 확인
- [ ] 크로스 브라우저 테스트
- [ ] 모바일 디바이스 테스트

### 배포 후
- [ ] 프로덕션 모니터링
- [ ] 사용자 피드백 수집
- [ ] 버그 트래킹
- [ ] 성능 모니터링

---

## 🎉 마무리

이 문서는 **반 배정 프로그램 Pro**의 웹앱 개발을 위한 종합 사양서입니다. 네오 부루탈리즘 디자인, Firebase 백엔드, Gemini AI 통합, 그리고 Vercel 배포를 통해 교사들에게 최고의 학급 편성 경험을 제공하는 것이 목표입니다.

**개발 기간**: 약 12주 (3개월)
**예상 출시일**: Phase 12 완료 후

모든 단계를 체계적으로 진행하고, 보안과 성능을 최우선으로 고려하며, 사용자 중심의 디자인을 유지합니다.

**Let's build something amazing! 🚀**
